<p style="color: green"><%= notice %></p>

<div class="container mt-3 ms-5">
  <h1 class="fs-1">Products</h1>
    <div id="products">
      <% @products.each do |product| %>
        <%= render product %>
        <p>
          <%= link_to "Show this product", product %>
        </p>
        <hr>
      <% end %>
    </div>
  </div>
<%= link_to "New product", new_product_path, class: "mb-5" %>
<br><br>

